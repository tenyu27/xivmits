# node_modules, not Plug'n'Play: Vite, Babel, and oxlint all resolve from a real
# node_modules tree, and the site is a plain static build - PnP would buy
# nothing here and cost tooling compatibility.
nodeLinker: node-modules
